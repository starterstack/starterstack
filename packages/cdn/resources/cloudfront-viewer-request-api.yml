Type: AWS::CloudFront::Function
Properties:
  Name: !Sub '${AWS::StackName}-viewer-request-api'
  AutoPublish: true
  FunctionCode: ${file(./resources/cloudfront-functions.js):requestApi}
  FunctionConfig:
    Comment: !Sub 'Created by Serverless Framework by ${AWS::StackName}'
    Runtime: cloudfront-js-1.0
